<%= content_for :html_title do %><%= @podcast.title %><% end %>
<%= content_for :body_class do %>application podcast<% end %>
<%= content_for :back_link do %><%= link_to '« All Podcasts', podcasts_path(), :class => 'white' %><% end %>
<%= content_for :art do %><%= @podcast.art_url_wide %><% end %>

<div class="podcast-wrapper">

  <h1 class="podcast-title">
    <%= @podcast.title %>
  </h1>

  <div class="podcast-body">
    <%= @podcast.body %>
  </div>

  <div class="podcast-episodes">
    <%= render 'episodes' %>
  </div>

</div>

<%= content_for :footer_links do %>
  <%= link_to 'Subscribe in iTunes', @podcast.subscribe_itunes_link %>
  <%= link_to 'Subscribe in RSS', podcast_rss_path(@podcast.shortname) %>
  <%= process_markdown(@podcast.links) %>
<% end %>